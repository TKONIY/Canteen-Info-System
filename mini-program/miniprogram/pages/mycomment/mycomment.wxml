  <van-tabs swipeable active="{{ active }}" bind:change="onChange">
    <van-tab title="评价">
    <scroll-view class="list">
   <!---->
      <view class="CONTAINER" wx:for="{{commentList}}"  >

        <view class="container1">

         <image src="/images/user-unlogin.png" class="chathead"></image>

         <view class="container">
            <text class="name">{{name}}</text>         
            <text class="time">{{item.time}}</text>

         </view>
         
        </view>
    
        <view class="commenttext">
          <text>{{item.text}}
          {{item.score}}</text>
        </view>

        <view class="container2">
          <text class="dishname">{{item.name}}</text>
          <text>{{item.window}} {{item.canteen}}</text>
        </view>
    

      </view>


    </scroll-view>

    </van-tab>

    <van-tab title="窗口">内容 2</van-tab>
  </van-tabs>
